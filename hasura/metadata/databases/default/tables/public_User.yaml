table:
  name: User
  schema: public
array_relationships:
  - name: Posts
    using:
      foreign_key_constraint_on:
        column: authorId
        table:
          name: Post
          schema: public
  - name: posts
    using:
      manual_configuration:
        column_mapping:
          id: authorId
        insertion_order: null
        remote_table:
          name: Post
          schema: public
